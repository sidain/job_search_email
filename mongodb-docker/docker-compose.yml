services:
  job-mongo:
    image: mongo:7
    container_name: job-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
    external: true
    name: mongodb-docker_mongo_data   # <-- the volume that already has your records
